as yet unclassified
initialize 

	bounds := self defaultBounds.
	text := ''.
	style := SSTStyle new.
	color := self defaultColor.
	backgroundColor := self defaultBackgroundColor.
	layout := SSTTextLayout new